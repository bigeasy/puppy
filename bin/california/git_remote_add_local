#!/bin/bash

repository=$1
cd $repository

UPSTREAM=ssh://alan@alvar.user.south.california.runpup.net/home/alan/git/ecma/puppy/janitor/.git

/usr/bin/git remote show | /bin/grep '^local$' > /dev/null
if [ $? -ne 0 ]
then
    /usr/bin/git remote add local $UPSTREAM
fi
