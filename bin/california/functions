function step() {
    local message=$1
    shift
    echo -n "$message"
    $@ 2&>1 1>/dev/null
    if [ $? -eq 0 ]
    then
        echo " [OK]"
    else
        echo " [FAILED]"
        exit 1
    fi
}
