#!/bin/bash
#
# This is the command to start up mongodb with authorization and the web conosle
# activated. Copy this to the directory where you've unzippped MongoDB and run.
#
# Place holder until I deploy 64 bit Linux.
#
# Shut down the server with a `kill -2`.

sudo -u mongo bin/mongod --fork --logpath /data/db/mongo.log --logappend --auth  --rest
