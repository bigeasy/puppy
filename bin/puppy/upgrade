#!/bin/bash

if [ $(whoami) = "root" ]; then
    echo "Must NOT run as root." 1>&2
    exit 1
fi

cd ~/.vim/bundle/vim-coffee-script
git pull

cd ~/.usr
git pull

cd ~/janitor
bin/puppy/checkout
bin/puppy/deploy

#sudo su janitor /home/janitor/janitor/bin/puppy/checkout

sudo yum update -y
