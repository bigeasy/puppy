#!/bin/bash

if [ $(/usr/bin/id -u) -ne 0 ]
then
    echo "Must run as root." 1>&2
    exit 1
fi

/bin/rpm -q postgresql-server > /dev/null
if [ $? -ne 0 ]
then
    /usr/bin/yum install -y postgresql-server
    /etc/init.d/postgresql initdb
fi
