#!/bin/bash -e

puppify module <<-help
  usage: puppify install [options] [package, package...]

  options:

help

[ $# -eq 0 ] && usage 0

for property in $@
do
  puppy_exec "configure-$property"
done
