#!/bin/bash

function abend()
{
    message=$1
    echo "ERROR: $message" 1>&1
    exit 1
}

[ $# -eq 1 -a -d $1 ] || abend "Must provide a working directory."

project=$(readlink -f $1)
user=$(basename $project)

cd $project

if [ -e Cakefile ]; then
    /usr/bin/cake compile
    /usr/bin/sudo /usr/bin/npm --globalconfig $HOME/git/ecma/puppy/system/bin/npmrc install .
    /usr/bin/sudo /sbin/restorecon -R /puppy
fi

function copy_file()
{
    source=$1
    dest=$2
    ownership=$3
    mode=$4
    file=$(basename $source)
    /bin/mkdir -p $dest
    install=0
    if [ -e $dest/$file ]; then
        if [ $(/usr/bin/stat --format=%Y $source) -gt $(/usr/bin/stat --format=%Y $dest/$file) ]; then
            install=1
        fi 
    else
        install=1
    fi
    if [ $install -eq 1 ]; then
        echo "Installing program $dest/$file."
        /bin/cp $source $dest/$file
    fi
    /bin/chmod $mode $dest/$file
    if [ $(/usr/bin/stat --format=%A $source | /usr/bin/cut -c 4) == "-" ]; then
        /bin/chmod gou-x $dest/$file
    fi
    /bin/chown $ownership $dest/$file
}

function copy_files()
{
    dir=$1
    if [ -e $dir ]; then
        dest=$2
        ownership=$3
        mode=$4
        for file in $(/bin/ls $dir); do
            copy_file $dir/$file $dest $ownership $mode
        done
    fi
}

echo $user | grep '^public|protected|private$'
if [ $user == "system" ]; then
    user=root
    mode=755
elif [ ! -z $(echo $user | grep '^public|protected|private$') ]; then
    user=puppy
    mode=550
else
    mode=550
fi

copy_files puppy/bin /puppy/bin $user:$user $mode
copy_files puppy/sbin /puppy/sbin $user:$user $mode

if [ -d src ]; then
    for coffee in $(ls src/*.coffee); do
        program=$(echo $coffee | /bin/sed 's/src\/\(.*\).coffee$/\1/')
        if [ -e /puppy/bin/$program ]; then
            /bin/chmod $mode /puppy/bin/$program*
            /bin/chown $user:$user /puppy/bin/$program*
        fi
    done
fi

/sbin/restorecon -R /puppy
